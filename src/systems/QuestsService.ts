export class QuestsService {
  async progressFromRound(results: any[]) {
    // TODO(Hytopia):
    // - weekly quests: survive X seconds, play N rounds, finish once
    // - reset cadence: weekKey (YYYY-Www)
    // - UI hooks: progress ticks on results screen
    return;
  }
}
